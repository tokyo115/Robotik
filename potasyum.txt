int pot = A0;
int led = 9;

void setup() {
  pinMode(led, OUTPUT);
  Serial.begin(9600);
}

void loop() {
  int deger = analogRead(pot);
  Serial.println(deger);

  int pwm = map(deger, 0, 1023, 0, 255);
  analogWrite(led, pwm);

  delay(200);
}
